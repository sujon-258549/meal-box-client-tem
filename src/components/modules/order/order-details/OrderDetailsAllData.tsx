/* eslint-disable @typescript-eslint/no-explicit-any */
const OrderDetailsAllData = ({ order }: { order: any }) => {
  console.log(order);
  return <div>OrderDetails</div>;
};

export default OrderDetailsAllData;
